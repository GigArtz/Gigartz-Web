
// Handle like
export const handleLike = (uid: string, eventId: string) => {
    console.log("Liked", eventId);
};

// Show comments
export const showComments = (eventId: string) => {
    console.log("Show comments", eventId);
};

// Show comments
export const shareEvent = (eventId: string) => {
    console.log("Show comments", eventId);
};
